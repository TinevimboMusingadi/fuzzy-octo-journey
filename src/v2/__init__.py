"""V2.0 entrypoint helpers and form registry.

This package adds a cleaner surface for:
- Selecting a pre-built form by ID
- Creating a new intake session

It reuses the existing graph/nodes/modes/validation from the root `src` package.
"""


